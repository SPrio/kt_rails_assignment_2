<% @images.each do |image| %>
  <%= link_to image_tag(image.avatar.url(:medium)), image.avatar.url %>
<% end %>
</br>
<%= link_to "Add new Image", new_image_path %>
</br>
<%= link_to "Go back to Home", root_path %>
